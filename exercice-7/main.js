/* Exo : créer un profil pour un personnage, le nom de la variable qu’on veut, ça doit être un objet, 
au moins 3 clés : prenom, nom, age, et afficher dans la console une phrase avec console.log (prenom et nom a age et est à l’IIM.*/
    
let ProfilPerso = {
    FirstName : "Miss",
    LastName : "Fortune",
    Age : 15,
}

console.log(ProfilPerso.FirstName + " " + ProfilPerso.LastName + " a " + ProfilPerso.Age + " ans" + " et est à l'école.")